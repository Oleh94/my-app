let firstState = {
    friendsData: [
        {id: 1, name: "ya"},
        {id: 2, name: "ty"},
        {id: 3, name: "vin"}
    ]
}

const reducerNavbar = (state=firstState, action=null)=> {
    return state;
    }

export default reducerNavbar;